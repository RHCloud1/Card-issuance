services:
  card-issuance:
    build: .
    container_name: card-issuance
    restart: unless-stopped
    env_file:
      - .env
    dns:
      - "${CONTAINER_DNS_PRIMARY:-1.1.1.1}"
      - "${CONTAINER_DNS_SECONDARY:-8.8.8.8}"
    expose:
      - "8080"
    volumes:
      - ./data:/data
